when HTTP_REQUEST {
                  if { [active_members [LB::server pool]] == 0 } {
                  HTTP::redirect "http://magnetdijital.com"
                                                                 }
                  }
